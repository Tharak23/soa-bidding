# BidVelocity frontend

Next.js App Router floor. API is the Spring gateway on port 8080.

## Env

Copy `.env.example` to `.env.local`.

```bash
npm install
npm run dev
```

App: `http://localhost:3000`. Sign in, onboard, then Marketplace / Wallet / Sell.
